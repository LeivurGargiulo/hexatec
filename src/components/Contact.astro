  <section id="contacto" class="py-16 bg-base-100">
    <div class="container mx-auto px-4 max-w-3xl" data-aos="fade_in">
      <h2 class="text-3xl font-bold text-primary text-center mb-6">
        Contactanos
      </h2>
      <p class="text-center mb-8">
        ¿Querés cotizar una instalación? ¿Tenés dudas? Escribinos sin
        compromiso.
      </p>

      <form class="grid gap-4">
        <input
          type="text"
          placeholder="Tu nombre"
          class="input input-bordered w-full"
        />
        <input
          type="email"
          placeholder="Tu email"
          class="input input-bordered w-full"
        />
        <textarea
          placeholder="Tu mensaje"
          class="textarea textarea-bordered w-full h-32"></textarea>
        <button class="btn btn-primary w-full">Enviar mensaje</button>
      </form>
    </div>
  </section>